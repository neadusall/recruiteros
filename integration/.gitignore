node_modules/
.next/
next-env.d.ts
*.tsbuildinfo
.env
.env.local
# The published owner-console page is regenerated at build (never commit it).
# The slug itself IS committed (in .owner-console-slug) so the URL is stable and
# known across deploys; override it per-environment with OWNER_CONSOLE_SLUG.
public/owner-*.html
