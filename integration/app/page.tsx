// The bare root "/" is redirected to /index.html by next.config.js, so this
// component is effectively never shown. Kept minimal as a safe fallback.
export default function Home() {
  return null;
}
